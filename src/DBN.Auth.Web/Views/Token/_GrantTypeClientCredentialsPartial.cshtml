@model TokenViewModelClientCredentials

@using (Html.BeginForm("ClientCredentials", "Token", FormMethod.Post))
{
    @Html.HiddenFor(m => m.GrantType)

    <tr>
        <td>@Html.LabelFor(m => m.Audience)</td>
        <td>
            @Html.TextBoxFor(m => m.Audience)
        </td>
    </tr>

    <tr>
        <td></td>
        <td>
            <input type="submit" name="command" value="get token" />
        </td>
    </tr>
}
